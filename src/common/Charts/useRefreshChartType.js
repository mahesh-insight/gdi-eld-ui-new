import { useCallback } from 'react';

const useRefreshChartType = () => {
  const refreshChartType = useCallback(async () => {
    return;
  }, []);

  return refreshChartType;
};

export default useRefreshChartType;